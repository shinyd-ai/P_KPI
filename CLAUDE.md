@AGENTS.md

## Claude Compatibility Note

Codex instructions for this repository live in `AGENTS.md`.

The old Claude responsive harness under `.claude/` is kept as reference only:

- Legacy orchestrator: `/responsive-orchestrator`
- Legacy agents: `responsive-analyzer`, `responsive-designer`,
  `responsive-implementer`, `responsive-verifier`
- Legacy artifacts: `.claude/artifacts/`

For Codex, use the workflow in `AGENTS.md` instead. Broad responsive work should
use `.codex/artifacts/` for new analysis, design, implementation, and
verification notes when artifacts are useful.
